type Network = "Mainnet" | "Testnet";

export const explorerLink = ({
  address,
  network,
}: {
  address: string;
  network: Network;
}) => {

  if()
  console.log({ address, network });
  return "";
};
